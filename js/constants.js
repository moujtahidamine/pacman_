const TAILLE_TUILE = 15; //  la taille des tuiles en px

const PACMAN_ID = "1"; // pacman id

const RUN_INTERVAL = 300; // 0.3 seconde

const GHOST_RUN_INTERVAL = 4000; // 4 seconde

const DIRECTIONS = [
  Direction.NORTH,
  Direction.WEST,
  Direction.EAST,
  Direction.SOUTH
];
